# React + Vite

This template provides a minimal setup to get React working in Vite with HMR and some ESLint rules.

Currently, two official plugins are available:

- [@vitejs/plugin-react](https://github.com/vitejs/vite-plugin-react/blob/main/packages/plugin-react/README.md) uses [Babel](https://babeljs.io/) for Fast Refresh
- [@vitejs/plugin-react-swc](https://github.com/vitejs/vite-plugin-react-swc) uses [SWC](https://swc.rs/) for Fast Refresh


## TO-DO list
- Login, sign up forms:
  - input validaton using regex, html pattern and title for tooltip
  - update validation in the backend
  - use a single change handler for all
  - use the context manager to write repititve functions for fetching data, cache refreshing - fewer fetches
- add course, profile:
    - add edit button
  - update layout
  - use context manager to manage the context edit mode and browser mode.
  - fetch on update, new, delete - invalidate the cache...
  - 
